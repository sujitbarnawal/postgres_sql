#connect to postgres user
psql -U postgres

#switch to table dvdrental
\c dvdrental


SELECT first_name FROM customer;
SELECT
   first_name,
   last_name,
   email
FROM
   customer;

SELECT * FROM customer;
SELECT
   first_name || ' ' || last_name as full_name,
   email
FROM
   customer;

SELECT NOW()
